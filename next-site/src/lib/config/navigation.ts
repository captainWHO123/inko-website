import { navigationSettings, policyLinks } from "@/content/fallback/site";

export const primaryNavigation = navigationSettings.primary;
export const footerNavigation = navigationSettings.footer;
export const sharedPolicyLinks = policyLinks;
